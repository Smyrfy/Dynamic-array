#include <iostream>

int main() {
    int data[] = {1, 2, 3, 4};
    int size = 4, index = 1; // Видаляємо '2'

    for (int i = index; i < size - 1; i++) data[i] = data[i+1];
    size--;

    for (int i = 0; i < size; i++) std::cout << data[i] << " ";
    return 0;
}
