#include <iostream>
#include <vector> // Використовуємо std::vector для простоти запуску

int main() {
    std::vector<int> v;
    int n;
    while(std::cin >> n && n != 0) v.push_back(n);
    for(int i = v.size()-1; i>=0; i--) std::cout << v[i] << " ";
    return 0;
}
