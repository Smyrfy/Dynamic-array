#include <iostream>

int main() {
    int a[] = {1, 2, 3}, b[] = {1, 2, 3};
    int s1 = 3, s2 = 3;
    bool eq = (s1 == s2);
    if(eq) for(int i=0; i<s1; i++) if(a[i] != b[i]) eq = false;
    std::cout << (eq ? "Equal" : "Not equal");
    return 0;
}
